## What happens to lines, grids and shapes? {#geometry-under-action}

A matrix acts point by point, but the results are organized.

### Lines through the origin remain lines through the origin

A line generated by a vector $\mathbf v$ has the form

$$
\mathbf x(t)=t\mathbf v.
$$

After applying $A$,

$$
A\mathbf x(t)=A(t\mathbf v)=t(A\mathbf v).
$$

The transformed points are generated by the single direction $A\mathbf v$. If $A\mathbf v\ne\mathbf0$, the result is another line through the origin. If $A\mathbf v=\mathbf0$, the whole line collapses to the origin.

### General lines remain lines or collapse

A line

$$
\mathbf x(t)=\mathbf p+t\mathbf v
$$

is transformed into

$$
A\mathbf x(t)=A\mathbf p+tA\mathbf v.
$$

This is again a point plus a direction, unless the direction is sent to zero.

### Parallel grid lines remain parallel or merge

All lines with direction $\mathbf v$ are transformed using direction $A\mathbf v$. They therefore stay parallel when $A\mathbf v\ne\mathbf0$. Several distinct lines may merge when the transformation loses a direction.

### Polygons are transformed by their vertices

For a triangle with vertices $\mathbf p$, $\mathbf q$ and $\mathbf r$, calculate

$$
A\mathbf p,\qquad A\mathbf q,\qquad A\mathbf r
$$

and join the three output points. Straight edges remain straight because the points of each edge are linear combinations of its endpoints.

!!! principle "Transform the structure, not isolated dots"
    A useful drawing should display at least two of the following together:

    - basis vectors;
    - coordinate grid;
    - a simple figure;
    - selected input and output points.

    Seeing only one transformed point hides the action on the plane.

!!! note "Area scaling is visible but postponed"
    The area of the transformed unit square is the area of the parallelogram generated by the two columns. A signed formula for this area is the determinant, which belongs to Lecture 07. Here we only observe whether the parallelogram expands, shrinks, flips orientation or collapses.
